# Spec 10 — WorkspaceChat
**Gali V5 · Panel conversacional multi-thread estilo Cursor/Claude Code**

> **Fecha:** 2026-06-10  
> **Autor:** Catalina Giraldo · catalina.giraldo@dropi.co  
> **Depende de:** Spec 8 (AuditoriaJun8) ejecutado  
> **Componente base:** `gali-right-panel.component`  
> **Skill a invocar antes de implementar:** `/interface-design`

---

## 1. Diagnóstico del Problema

Un solo hilo de conversación acumulativo no escala cuando el dropshipper necesita hablar de Collar GPS en un contexto, de Meta Ads en otro, y de novedades de Cali en un tercero. El modelo actual tiene tres fallas sistémicas:

**Falla 1 — Contexto contaminado**  
Mensajes sobre logística aparecen intercalados con mensajes sobre campañas. Si Gali responde "cambié el transportista" después de una pregunta sobre ROAS, el usuario pierde el hilo de por qué se hizo ese cambio.

**Falla 2 — Sin memoria de conversaciones anteriores**  
El usuario no puede retomar una conversación que tuvo hace 3 días sobre el proyecto de skincare. Todo el historial está mezclado en un único scroll infinito.

**Falla 3 — Respuestas solo texto cuando el negocio habla en datos**  
Cuando Gali reporta ROAS o novedades, lo hace en texto plano ("El ROAS es 1.93x"). No hay micro-gráficos de tendencia, no hay comparación visual antes/después, no hay botones de acción directa. El chat se siente como un bot de texto, no como un workspace de negocio.

---

## 2. Estado Actual del Componente

El `gali-right-panel.component` ya tiene una implementación parcial de multi-thread que sirve como base:

| Funcionalidad | Estado | Notas |
|---|---|---|
| `threads` signal con array de ChatThread | ✅ Existe | `threads = signal<ChatThread[]>(...)` en `.ts` línea 85 |
| `switchThread`, `archiveThread`, `createThread` | ✅ Existe | Métodos funcionales, líneas 103-126 |
| `pinnedThreadIds` signal | ✅ Existe | Línea 45 — pero **sin persistencia en localStorage** |
| Thread micro-rail (32px/168px hover) | ✅ Existe | `.gali-thread-rail` en SCSS |
| Thread drawer dropdown (dark) | ✅ Existe | `.gali-thread-drawer` en HTML/SCSS |
| Historial de mensajes por thread | ✅ Existe | Cada `ChatThread.messages[]` es independiente |
| Grouping Fijados / Por Proyecto / Por Agente | ❌ Falta | Thread list es lista plana sin secciones |
| Selector de contexto al crear thread | ❌ Falta | `createThread()` abre vacío sin contexto |
| Pre-carga desde card de proyecto | ❌ Falta | No hay integración con navegación |
| Pre-carga desde ficha de agente | ❌ Falta | No hay integración con `/gali-v5/agentes` |
| Rich cards en respuestas (ROAS, pedidos) | ❌ Falta | Messages solo tienen `text: string` |
| `pinThread` persiste en localStorage | ❌ Falta | Solo actualiza signal en memoria |

---

## 3. Tareas

### C1 — Ampliar `ChatThread` para soportar grouping y rich cards

**Archivo:** `gali-right-panel.component.ts`  
**Tipo:** Modificación de tipos existentes

Reemplazar la interfaz `ChatThread` y el tipo de mensaje inline por interfaces explícitas que soporten agrupación y rich cards:

```typescript
// Añadir al inicio del archivo, antes del @Component

export type ThreadCategory = 'proyecto' | 'agente' | 'libre';

export interface GaliRichCard {
  type: 'roas_chart' | 'pedidos_summary' | 'novedad_alert' | 'action_buttons';
  // roas_chart
  roasData?: { label: string; value: number; }[];  // últimos 7 días
  roasCurrent?: number;
  roasTarget?: number;
  // pedidos_summary
  pedidosCount?: number;
  pedidosNovedad?: number;
  pedidosPendientes?: number;
  // novedad_alert
  novedadPct?: number;
  novedadCiudad?: string;
  novedadTransportadora?: string;
  // action_buttons
  actions?: { label: string; action: string; isPrimary?: boolean }[];
}

export interface ChatMessage {
  id: string;
  from: 'user' | 'gali';
  text: string;
  time: string;
  richCard?: GaliRichCard;
}

export interface ChatThread {
  id: string;
  title: string;
  agente: string;
  agentColor: string;
  category: ThreadCategory;
  contextId?: string;      // proyectoId o agenteId según category
  contextLabel?: string;   // "Collar GPS" o "Roax"
  messages: ChatMessage[];
  unread: number;
  isPinned?: boolean;      // para persistencia en localStorage
  createdAt: number;
}
```

Actualizar la inicialización de `threads` para incluir los nuevos campos:

```typescript
readonly threads = signal<ChatThread[]>([
  {
    id: 'gali-main',
    title: 'Gali · Hub general',
    agente: 'Gali',
    agentColor: '#f49a3d',
    category: 'libre',
    messages: [],
    unread: 0,
    createdAt: Date.now(),
  },
  {
    id: 'roax-collar',
    title: 'Roax · Collar GPS',
    agente: 'Roax',
    agentColor: '#f97316',
    category: 'proyecto',
    contextId: 'collar-gps-2026',
    contextLabel: 'Collar GPS',
    messages: [
      {
        id: 'r1',
        from: 'gali',
        text: 'El CTR del creativo A cayó a 1.2%. Tengo listo un creativo de respaldo — ¿lo activo?',
        time: 'hace 10 min',
        richCard: {
          type: 'roas_chart',
          roasCurrent: 1.93,
          roasTarget: 2.5,
          roasData: [
            { label: 'L', value: 2.1 },
            { label: 'M', value: 2.3 },
            { label: 'X', value: 1.9 },
            { label: 'J', value: 1.8 },
            { label: 'V', value: 1.93 },
          ],
        },
      },
    ],
    unread: 1,
    createdAt: Date.now() - 3600000,
  },
  {
    id: 'vigilante-novedades',
    title: 'Vigilante · Novedades Bogotá',
    agente: 'Vigilante',
    agentColor: '#fbbf24',
    category: 'agente',
    contextId: 'vigilante',
    contextLabel: 'Vigilante',
    messages: [
      {
        id: 'v1',
        from: 'gali',
        text: 'Novedad en Bogotá subió al 18% esta semana. ¿Quieres que reasigne a Envia para los próximos 5 días?',
        time: 'hace 2h',
        richCard: {
          type: 'novedad_alert',
          novedadPct: 18,
          novedadCiudad: 'Bogotá',
          novedadTransportadora: 'Coordinadora',
        },
      },
    ],
    unread: 1,
    createdAt: Date.now() - 7200000,
  },
  {
    id: 'kronos-finanzas',
    title: 'Kronos · P&L semanal',
    agente: 'Kronos',
    agentColor: '#60a5fa',
    category: 'agente',
    contextId: 'kronos',
    contextLabel: 'Kronos',
    messages: [
      {
        id: 'k1',
        from: 'gali',
        text: 'Esta semana cerraste con $2.74M en ingresos. El margen neto bajó 2pts por el incremento en fletes.',
        time: 'hoy 8am',
        richCard: {
          type: 'pedidos_summary',
          pedidosCount: 47,
          pedidosNovedad: 3,
          pedidosPendientes: 8,
        },
      },
    ],
    unread: 0,
    createdAt: Date.now() - 14400000,
  },
]);
```

---

### C2 — Refactorizar el thread drawer para soportar secciones Fijados / Por Proyecto / Por Agente

**Archivo:** `gali-right-panel.component.ts` + `.html` + `.scss`  
**Tipo:** Modificación del drawer existente

#### TS — Computed signals para grouping:

```typescript
// Reemplazar el computed sortedThreads existente por estos tres:

readonly pinnedThreads = computed(() =>
  this.threads().filter(t => this.pinnedThreadIds().includes(t.id))
);

readonly projectThreads = computed(() =>
  this.threads().filter(
    t => t.category === 'proyecto' && !this.pinnedThreadIds().includes(t.id)
  )
);

readonly agentThreads = computed(() =>
  this.threads().filter(
    t => t.category === 'agente' && !this.pinnedThreadIds().includes(t.id)
  )
);

readonly freeThreads = computed(() =>
  this.threads().filter(
    t => t.category === 'libre' && !this.pinnedThreadIds().includes(t.id)
  )
);

// Filtrado de búsqueda aplicado a todos:
readonly filteredPinned = computed(() =>
  this.filterBySearch(this.pinnedThreads())
);
readonly filteredProjects = computed(() =>
  this.filterBySearch(this.projectThreads())
);
readonly filteredAgents = computed(() =>
  this.filterBySearch(this.agentThreads())
);
readonly filteredFree = computed(() =>
  this.filterBySearch(this.freeThreads())
);

private filterBySearch(threads: ChatThread[]): ChatThread[] {
  const q = this.threadSearch().toLowerCase().trim();
  return q ? threads.filter(t => t.title.toLowerCase().includes(q)) : threads;
}
```

#### HTML — Reemplazar el contenido de `.gali-thread-drawer__list`:

```html
<!-- Dentro de .gali-thread-drawer, reemplazar la sección __list -->
<div class="gali-thread-drawer__list">

  <!-- SECCIÓN: FIJADOS -->
  @if (filteredPinned().length > 0) {
    <div class="gali-thread-section">
      <div class="gali-thread-section__label">
        <i class="pi pi-bookmark-fill"></i> FIJADOS
      </div>
      @for (t of filteredPinned(); track t.id) {
        <ng-container *ngTemplateOutlet="threadItem; context: { $implicit: t }"></ng-container>
      }
    </div>
  }

  <!-- SECCIÓN: POR PROYECTO -->
  @if (filteredProjects().length > 0) {
    <div class="gali-thread-section">
      <div class="gali-thread-section__label">
        <i class="pi pi-briefcase"></i> POR PROYECTO
      </div>
      @for (t of filteredProjects(); track t.id) {
        <ng-container *ngTemplateOutlet="threadItem; context: { $implicit: t }"></ng-container>
      }
    </div>
  }

  <!-- SECCIÓN: POR AGENTE -->
  @if (filteredAgents().length > 0) {
    <div class="gali-thread-section">
      <div class="gali-thread-section__label">
        <i class="pi pi-bolt"></i> POR AGENTE
      </div>
      @for (t of filteredAgents(); track t.id) {
        <ng-container *ngTemplateOutlet="threadItem; context: { $implicit: t }"></ng-container>
      }
    </div>
  }

  <!-- SECCIÓN: SIN CATEGORÍA -->
  @if (filteredFree().length > 0) {
    <div class="gali-thread-section">
      <div class="gali-thread-section__label">SIN CATEGORÍA</div>
      @for (t of filteredFree(); track t.id) {
        <ng-container *ngTemplateOutlet="threadItem; context: { $implicit: t }"></ng-container>
      }
    </div>
  }

  @if (threadSearch() && filteredPinned().length === 0 && filteredProjects().length === 0 && filteredAgents().length === 0 && filteredFree().length === 0) {
    <div class="gali-thread-drawer__empty">Sin resultados</div>
  }
</div>

<!-- Template reutilizable para cada thread item -->
<ng-template #threadItem let-t>
  <button type="button" class="gali-thread-drawer__item"
    [class.gali-thread-drawer__item--active]="activeThreadId() === t.id"
    (click)="switchThread(t.id); showThreadList.set(false)"
    data-proto-skip>
    <span class="gali-thread-drawer__dot" [style.background]="t.agentColor"></span>
    <div class="gali-thread-drawer__body">
      <span class="gali-thread-drawer__title">{{ t.title }}</span>
      @if (t.contextLabel) {
        <span class="gali-thread-drawer__context">{{ t.contextLabel }}</span>
      }
    </div>
    <div class="gali-thread-drawer__item-actions">
      <span class="gali-thread-drawer__pin-btn"
        (click)="pinThread(t.id); $event.stopPropagation()"
        [title]="pinnedThreadIds().includes(t.id) ? 'Desfijar' : 'Fijar'">
        <i class="pi" [class.pi-bookmark-fill]="pinnedThreadIds().includes(t.id)" [class.pi-bookmark]="!pinnedThreadIds().includes(t.id)"></i>
      </span>
      @if (t.unread > 0) {
        <span class="gali-thread-drawer__unread">{{ t.unread }}</span>
      }
    </div>
  </button>
</ng-template>
```

#### SCSS — Estilos para secciones dentro del drawer:

```scss
// Añadir a gali-right-panel.component.scss

.gali-thread-section {
  &__label {
    display: flex;
    align-items: center;
    gap: 5px;
    padding: 8px 16px 4px;
    font-family: $font-primary;
    font-size: 9px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: rgba(255, 255, 255, 0.3);

    .pi {
      font-size: 8px;
      opacity: 0.5;
    }
  }
}

.gali-thread-drawer {
  &__item-actions {
    display: flex;
    align-items: center;
    gap: 4px;
    flex-shrink: 0;
  }

  &__pin-btn {
    opacity: 0;
    width: 18px;
    height: 18px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 3px;
    cursor: pointer;
    transition: opacity 0.12s, background 0.12s;
    color: rgba(255, 255, 255, 0.4);

    .pi { font-size: 9px; }
    &:hover { color: $primary-500; background: rgba($primary-500, 0.15); }
  }

  &__item:hover &__pin-btn { opacity: 1; }

  &__empty {
    padding: 12px 16px;
    font-family: $font-primary;
    font-size: 11px;
    color: rgba(255, 255, 255, 0.25);
    text-align: center;
  }
}
```

---

### C3 — Selector de contexto al crear un nuevo thread

**Archivo:** `gali-right-panel.component.ts` + `.html` + `.scss`  
**Tipo:** Nuevo inline widget dentro del componente existente

Cuando el usuario hace clic en "+ Nuevo", en lugar de crear el thread inmediatamente, mostrar un selector de contexto inline dentro del drawer.

#### TS:

```typescript
// Añadir al componente:
readonly showContextSelector = signal(false);
readonly newThreadCategory = signal<ThreadCategory | null>(null);
readonly newThreadContextId = signal<string | null>(null);
readonly newThreadContextLabel = signal('');

// Datos mock para el selector (proyectos y agentes disponibles)
readonly availableProjects = [
  { id: 'collar-gps-2026', label: 'Collar GPS para Mascotas', agentColor: '#f97316' },
  { id: 'skincare-kbeauty', label: 'Skincare K-Beauty', agentColor: '#a78bfa' },
  { id: 'difusor-aroma', label: 'Difusor Aromaterapia', agentColor: '#34d399' },
];

readonly availableAgents = [
  { id: 'roax', label: 'Roax', desc: 'Campañas y ROAS', color: '#f97316' },
  { id: 'vigilante', label: 'Vigilante', desc: 'Novedades y logística', color: '#fbbf24' },
  { id: 'kronos', label: 'Kronos', desc: 'P&L y finanzas', color: '#60a5fa' },
  { id: 'chatea', label: 'Chatea Pro', desc: 'WhatsApp y atención', color: '#34d399' },
  { id: 'ada', label: 'Ada', desc: 'Catálogo y precios', color: '#818cf8' },
];

openContextSelector(): void {
  this.showContextSelector.set(true);
  this.newThreadCategory.set(null);
  this.newThreadContextId.set(null);
  this.newThreadContextLabel.set('');
}

selectCategory(category: ThreadCategory): void {
  this.newThreadCategory.set(category);
  if (category === 'libre') {
    this.confirmCreateThread();
  }
}

selectContext(id: string, label: string): void {
  this.newThreadContextId.set(id);
  this.newThreadContextLabel.set(label);
  this.confirmCreateThread();
}

confirmCreateThread(): void {
  const category = this.newThreadCategory() ?? 'libre';
  const contextId = this.newThreadContextId() ?? undefined;
  const contextLabel = this.newThreadContextLabel() || undefined;

  // Determinar agente y color según categoría y contexto
  let agente = 'Gali';
  let agentColor = '#f49a3d';
  if (category === 'agente' && contextId) {
    const found = this.availableAgents.find(a => a.id === contextId);
    if (found) { agente = found.label; agentColor = found.color; }
  }

  const title = contextLabel
    ? `${agente} · ${contextLabel}`
    : 'Nueva conversación';

  const id = `thread-${Date.now()}`;
  const newThread: ChatThread = {
    id, title, agente, agentColor, category,
    contextId, contextLabel,
    messages: [],
    unread: 0,
    createdAt: Date.now(),
  };

  this.threads.update(list => [...list, newThread]);
  this.switchThread(id);
  this.showContextSelector.set(false);
}
```

#### HTML — Selector inline dentro del drawer, reemplaza el botón `__new`:

```html
<!-- Reemplazar el botón gali-thread-drawer__new por esto: -->
@if (showContextSelector()) {
  <div class="gali-ctx-selector">
    <div class="gali-ctx-selector__header">Nueva conversación</div>

    @if (!newThreadCategory()) {
      <div class="gali-ctx-selector__categories">
        <button type="button" class="gali-ctx-selector__cat"
          (click)="selectCategory('proyecto')" data-proto-skip>
          <i class="pi pi-briefcase"></i>
          <span>Por proyecto</span>
          <i class="pi pi-chevron-right gali-ctx-selector__chevron"></i>
        </button>
        <button type="button" class="gali-ctx-selector__cat"
          (click)="selectCategory('agente')" data-proto-skip>
          <i class="pi pi-bolt"></i>
          <span>Por agente</span>
          <i class="pi pi-chevron-right gali-ctx-selector__chevron"></i>
        </button>
        <button type="button" class="gali-ctx-selector__cat gali-ctx-selector__cat--free"
          (click)="selectCategory('libre')" data-proto-skip>
          <i class="pi pi-comments"></i>
          <span>Libre (sin contexto)</span>
        </button>
      </div>
    }

    @if (newThreadCategory() === 'proyecto') {
      <div class="gali-ctx-selector__items">
        @for (p of availableProjects; track p.id) {
          <button type="button" class="gali-ctx-selector__item"
            (click)="selectContext(p.id, p.label)" data-proto-skip>
            <span class="gali-ctx-selector__dot" [style.background]="p.agentColor"></span>
            {{ p.label }}
          </button>
        }
      </div>
    }

    @if (newThreadCategory() === 'agente') {
      <div class="gali-ctx-selector__items">
        @for (a of availableAgents; track a.id) {
          <button type="button" class="gali-ctx-selector__item"
            (click)="selectContext(a.id, a.label)" data-proto-skip>
            <span class="gali-ctx-selector__dot" [style.background]="a.color"></span>
            <span class="gali-ctx-selector__item-info">
              <span>{{ a.label }}</span>
              <span class="gali-ctx-selector__item-desc">{{ a.desc }}</span>
            </span>
          </button>
        }
      </div>
    }

    <button type="button" class="gali-ctx-selector__cancel"
      (click)="showContextSelector.set(false)" data-proto-skip>
      Cancelar
    </button>
  </div>
} @else {
  <button type="button" class="gali-thread-drawer__new"
    (click)="openContextSelector()" data-proto-skip>
    <i class="pi pi-plus"></i>
    <span>Nueva conversación</span>
  </button>
}
```

#### SCSS:

```scss
// Añadir a gali-right-panel.component.scss

.gali-ctx-selector {
  border-top: 1px solid rgba(255, 255, 255, 0.06);
  padding: 8px 0 4px;
  animation: threadDrawerIn 0.15s ease;

  &__header {
    padding: 4px 16px 8px;
    font-family: $font-primary;
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: rgba(255, 255, 255, 0.3);
  }

  &__categories {
    display: flex;
    flex-direction: column;
    gap: 1px;
    padding: 0 6px;
  }

  &__cat {
    display: flex;
    align-items: center;
    gap: 8px;
    width: 100%;
    padding: 7px 10px;
    background: none;
    border: 1px solid transparent;
    border-radius: 6px;
    color: rgba(255, 255, 255, 0.7);
    font-family: $font-primary;
    font-size: 12px;
    cursor: pointer;
    text-align: left;
    transition: background 0.12s, color 0.12s;

    .pi { font-size: 12px; opacity: 0.6; }

    &:hover {
      background: rgba(255, 255, 255, 0.06);
      color: rgba(255, 255, 255, 0.95);
    }

    &--free {
      margin-top: 2px;
      border-top: 1px solid rgba(255, 255, 255, 0.06);
      padding-top: 8px;
      color: rgba(255, 255, 255, 0.35);
      font-size: 11px;
    }
  }

  &__chevron {
    margin-left: auto !important;
    font-size: 9px !important;
    opacity: 0.4 !important;
  }

  &__items {
    display: flex;
    flex-direction: column;
    gap: 1px;
    padding: 0 6px;
    animation: threadDrawerIn 0.12s ease;
  }

  &__item {
    display: flex;
    align-items: center;
    gap: 8px;
    width: 100%;
    padding: 7px 10px;
    background: none;
    border: none;
    border-radius: 6px;
    color: rgba(255, 255, 255, 0.75);
    font-family: $font-primary;
    font-size: 12px;
    cursor: pointer;
    text-align: left;
    transition: background 0.12s, color 0.12s;

    &:hover {
      background: rgba(255, 255, 255, 0.06);
      color: rgba(255, 255, 255, 0.95);
    }
  }

  &__dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
  }

  &__item-info {
    display: flex;
    flex-direction: column;
    gap: 1px;
    min-width: 0;
  }

  &__item-desc {
    font-size: 10px;
    color: rgba(255, 255, 255, 0.35);
  }

  &__cancel {
    width: 100%;
    padding: 7px 16px;
    background: none;
    border: none;
    border-top: 1px solid rgba(255, 255, 255, 0.06);
    color: rgba(255, 255, 255, 0.25);
    font-family: $font-primary;
    font-size: 11px;
    cursor: pointer;
    text-align: center;
    margin-top: 4px;
    transition: color 0.12s;

    &:hover { color: rgba(255, 255, 255, 0.6); }
  }
}
```

---

### C4 — Confirmar: historial independiente por thread (ya implementado)

**Archivo:** `gali-right-panel.component.ts`  
**Tipo:** Verificación — no requiere código nuevo

El historial independiente ya existe. Verificar que:
1. `send()` (línea 239) escribe en `this.activeThreadId()` correctamente para threads no-main
2. El HTML renderiza `activeThread().messages` para threads distintos de `gali-main`

**Única adición necesaria** — agregar soporte de rich cards al render de mensajes de threads no-main (ver C7).

---

### C5 — Thread pre-cargado al abrir desde card de proyecto

**Archivo:** `gali-right-panel.component.ts` + `gali-state.service.ts`  
**Tipo:** Nuevo método público + input signal

El panel necesita exponer un método para recibir un proyectoId desde fuera y abrir o crear el thread correspondiente:

```typescript
// En gali-right-panel.component.ts

// Input signal para abrir thread por proyecto desde componentes externos
readonly pendingProjectThread = signal<{ projectId: string; projectLabel: string } | null>(null);

// Método público (llamado desde project cards vía GaliStateService)
openProjectThread(projectId: string, projectLabel: string): void {
  // Buscar thread existente para este proyecto
  const existing = this.threads().find(
    t => t.category === 'proyecto' && t.contextId === projectId
  );

  if (existing) {
    this.switchThread(existing.id);
  } else {
    // Crear thread con contexto del proyecto pre-cargado
    const id = `proj-${projectId}-${Date.now()}`;
    const newThread: ChatThread = {
      id,
      title: `Gali · ${projectLabel}`,
      agente: 'Gali',
      agentColor: '#f49a3d',
      category: 'proyecto',
      contextId: projectId,
      contextLabel: projectLabel,
      messages: [
        {
          id: 'ctx-preload',
          from: 'gali',
          text: `Abrí el contexto de ${projectLabel}. Tengo los datos actuales de campaña, ROAS y novedades. ¿Qué quieres revisar?`,
          time: 'ahora',
          richCard: {
            type: 'action_buttons',
            actions: [
              { label: 'Ver ROAS', action: `ask_roas_${projectId}` },
              { label: 'Ver novedades', action: `ask_novedades_${projectId}` },
              { label: 'Ver pedidos', action: `ask_pedidos_${projectId}`, isPrimary: true },
            ],
          },
        },
      ],
      unread: 0,
      createdAt: Date.now(),
    };
    this.threads.update(list => [...list, newThread]);
    this.switchThread(id);
  }

  this.activeTab.set('chat');
}
```

En `gali-state.service.ts`, añadir el request de thread de proyecto:

```typescript
// En gali-state.service.ts — añadir junto a requestedPanelTab
readonly requestedProjectThread = signal<{ projectId: string; projectLabel: string } | null>(null);
```

En el constructor del `GaliRightPanelComponent`, añadir efecto para escuchar:

```typescript
// En el constructor, junto al effect existente de requestedPanelTab:
effect(() => {
  const req = this.gali.requestedProjectThread();
  if (req) {
    this.openProjectThread(req.projectId, req.projectLabel);
    this.gali.requestedProjectThread.set(null);
  }
}, { allowSignalWrites: true });
```

Desde cualquier card de proyecto, invocar así:

```typescript
// Ejemplo en proyecto-card.component.ts
openInGali(): void {
  this.gali.requestedProjectThread.set({
    projectId: this.project.id,
    projectLabel: this.project.nombre,
  });
  // Asegurar que el panel está abierto
  this.gali.panelOpen.set(true);
}
```

---

### C6 — Thread pre-cargado al abrir desde ficha de agente

**Archivo:** `gali-right-panel.component.ts` + `gali-state.service.ts`  
**Tipo:** Extensión del patrón de C5

Siguiendo el mismo patrón que C5, para agentes:

```typescript
// En gali-state.service.ts
readonly requestedAgentThread = signal<{ agentId: string; agentLabel: string; agentColor: string } | null>(null);
```

```typescript
// En gali-right-panel.component.ts

openAgentThread(agentId: string, agentLabel: string, agentColor: string): void {
  const existing = this.threads().find(
    t => t.category === 'agente' && t.contextId === agentId
  );

  if (existing) {
    this.switchThread(existing.id);
  } else {
    // Buscar acciones recientes del agente (del beforeAfterLog o mock)
    const recentActions = this.beforeAfterLog
      .filter(e => e.agente.toLowerCase() === agentLabel.toLowerCase())
      .slice(0, 2);

    const recentText = recentActions.length
      ? `Últimas acciones de ${agentLabel}: ${recentActions.map(a => a.accion).join(' · ')}`
      : `${agentLabel} está activo. ¿Qué quieres revisar o configurar?`;

    const id = `agent-${agentId}-${Date.now()}`;
    const newThread: ChatThread = {
      id,
      title: `${agentLabel} · Configuración`,
      agente: agentLabel,
      agentColor,
      category: 'agente',
      contextId: agentId,
      contextLabel: agentLabel,
      messages: [
        {
          id: 'agent-preload',
          from: 'gali',
          text: recentText,
          time: 'ahora',
          richCard: {
            type: 'action_buttons',
            actions: [
              { label: 'Ver historial', action: `agent_history_${agentId}` },
              { label: 'Configurar umbral', action: `agent_config_${agentId}` },
              { label: 'Ver impacto', action: `agent_impact_${agentId}`, isPrimary: true },
            ],
          },
        },
      ],
      unread: 0,
      createdAt: Date.now(),
    };
    this.threads.update(list => [...list, newThread]);
    this.switchThread(id);
  }

  this.activeTab.set('chat');
}

// En el constructor, añadir effect:
effect(() => {
  const req = this.gali.requestedAgentThread();
  if (req) {
    this.openAgentThread(req.agentId, req.agentLabel, req.agentColor);
    this.gali.requestedAgentThread.set(null);
  }
}, { allowSignalWrites: true });
```

---

### C7 — Rich cards en respuestas de Gali

**Archivo:** `gali-right-panel.component.ts` + `.html` + `.scss`  
**Tipo:** Nuevo componente inline de rich cards

#### HTML — Añadir render de richCard dentro del bloque de mensajes (tanto gali-main como threads secundarios):

```html
<!-- Dentro de .gali-msg__bubble-wrap, DESPUÉS del @if (msg.text): -->

@if (msg.richCard; as card) {
  <div class="gali-rich-card" [class]="'gali-rich-card--' + card.type">

    <!-- ROAS Chart -->
    @if (card.type === 'roas_chart' && card.roasData) {
      <div class="roas-card">
        <div class="roas-card__header">
          <span class="roas-card__label">ROAS tendencia</span>
          <span class="roas-card__current" [class.roas-card__current--low]="(card.roasCurrent ?? 0) < (card.roasTarget ?? 2.5)">
            {{ card.roasCurrent }}×
          </span>
        </div>
        <div class="roas-card__chart">
          @for (point of card.roasData; track $index; let i = $index) {
            <div class="roas-bar-wrap">
              <div class="roas-bar"
                [style.height.%]="(point.value / 3.5) * 100"
                [class.roas-bar--target]="point.value >= (card.roasTarget ?? 2.5)"
                [style.animation-delay.ms]="i * 60">
              </div>
              <span class="roas-bar__label">{{ point.label }}</span>
            </div>
          }
        </div>
        @if (card.roasTarget) {
          <div class="roas-card__target">
            Objetivo: {{ card.roasTarget }}× ·
            <span [class.roas-card__gap--low]="(card.roasCurrent ?? 0) < card.roasTarget"
                  [class.roas-card__gap--ok]="(card.roasCurrent ?? 0) >= card.roasTarget">
              {{ (card.roasCurrent ?? 0) >= card.roasTarget ? '✓ Alcanzado' : 'Faltan ' + ((card.roasTarget - (card.roasCurrent ?? 0)).toFixed(2)) + '×' }}
            </span>
          </div>
        }
      </div>
    }

    <!-- Resumen de pedidos -->
    @if (card.type === 'pedidos_summary') {
      <div class="pedidos-card">
        <div class="pedidos-stat">
          <span class="pedidos-stat__val">{{ card.pedidosCount }}</span>
          <span class="pedidos-stat__label">esta semana</span>
        </div>
        <div class="pedidos-card__sep"></div>
        <div class="pedidos-stat">
          <span class="pedidos-stat__val pedidos-stat__val--warn">{{ card.pedidosNovedad }}</span>
          <span class="pedidos-stat__label">novedades</span>
        </div>
        <div class="pedidos-card__sep"></div>
        <div class="pedidos-stat">
          <span class="pedidos-stat__val">{{ card.pedidosPendientes }}</span>
          <span class="pedidos-stat__label">pendientes</span>
        </div>
      </div>
    }

    <!-- Alerta de novedad -->
    @if (card.type === 'novedad_alert') {
      <div class="novedad-card">
        <div class="novedad-card__bar">
          <div class="novedad-card__fill" [style.width.%]="card.novedadPct ?? 0"
            [class.novedad-card__fill--crit]="(card.novedadPct ?? 0) >= 15"
            [class.novedad-card__fill--warn]="(card.novedadPct ?? 0) >= 8 && (card.novedadPct ?? 0) < 15">
          </div>
        </div>
        <div class="novedad-card__meta">
          <span class="novedad-card__pct"
            [class.novedad-card__pct--crit]="(card.novedadPct ?? 0) >= 15">
            {{ card.novedadPct }}% novedad
          </span>
          <span class="novedad-card__ciudad">{{ card.novedadCiudad }}</span>
          @if (card.novedadTransportadora) {
            <span class="novedad-card__carrier">{{ card.novedadTransportadora }}</span>
          }
        </div>
      </div>
    }

    <!-- Action buttons -->
    @if (card.type === 'action_buttons' && card.actions) {
      <div class="gali-msg__actions">
        @for (action of card.actions; track action.action) {
          <button type="button"
            class="gali-msg__action-btn"
            [class.gali-msg__action-btn--primary]="action.isPrimary"
            data-proto-skip
            (click)="executeAction(action.action)">
            {{ action.label }}
          </button>
        }
      </div>
    }

  </div>
}
```

**Importante:** Aplicar este bloque tanto en el `@if (activeThreadId() === 'gali-main')` como en el `@if (activeThreadId() !== 'gali-main')` del template.

#### SCSS — Estilos de rich cards:

```scss
// Añadir a gali-right-panel.component.scss

.gali-rich-card {
  margin-top: 6px;
  border-radius: $radius-md;
  overflow: hidden;
  animation: execCardIn 0.2s ease;
}

// ── ROAS Chart Card ──
.roas-card {
  background: $panel-surface;
  border: 1px solid $panel-border;
  border-radius: $radius-md;
  padding: 10px 12px;
  display: flex;
  flex-direction: column;
  gap: 8px;

  &__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  &__label {
    font-family: $font-primary;
    font-size: 10px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: $panel-muted;
  }

  &__current {
    font-family: $font-secondary;
    font-size: 18px;
    font-weight: 700;
    color: $panel-success;

    &--low { color: $panel-warning; }
  }

  &__chart {
    display: flex;
    align-items: flex-end;
    gap: 4px;
    height: 36px;
  }

  &__target {
    font-family: $font-primary;
    font-size: 10px;
    color: $panel-muted;
  }

  &__gap {
    &--low { color: $panel-warning; font-weight: 600; }
    &--ok  { color: $panel-success; font-weight: 600; }
  }
}

.roas-bar-wrap {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 3px;
  flex: 1;
}

.roas-bar {
  width: 100%;
  min-height: 3px;
  background: rgba($primary-500, 0.25);
  border-radius: 2px 2px 0 0;
  animation: gali-bar-grow 0.3s ease-out forwards;

  &--target { background: $panel-success; }
}

.roas-bar__label {
  font-family: $font-primary;
  font-size: 9px;
  color: $panel-caption;
}

// ── Pedidos Summary Card ──
.pedidos-card {
  display: flex;
  align-items: center;
  background: $panel-surface;
  border: 1px solid $panel-border;
  border-radius: $radius-md;
  overflow: hidden;

  &__sep {
    width: 1px;
    height: 36px;
    background: $panel-border;
    flex-shrink: 0;
  }
}

.pedidos-stat {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
  padding: 8px 4px;

  &__val {
    font-family: $font-secondary;
    font-size: 18px;
    font-weight: 700;
    color: $panel-text;
    line-height: 1;

    &--warn { color: $panel-warning; }
  }

  &__label {
    font-family: $font-primary;
    font-size: 9px;
    color: $panel-muted;
    text-align: center;
    line-height: 1.2;
  }
}

// ── Novedad Alert Card ──
.novedad-card {
  background: rgba(239, 68, 68, 0.06);
  border: 1px solid rgba(239, 68, 68, 0.2);
  border-radius: $radius-md;
  padding: 8px 10px;
  display: flex;
  flex-direction: column;
  gap: 6px;

  &__bar {
    height: 4px;
    background: rgba(0, 0, 0, 0.08);
    border-radius: 2px;
    overflow: hidden;
  }

  &__fill {
    height: 100%;
    border-radius: 2px;
    background: $panel-warning;
    transition: width 0.4s ease-out;

    &--warn { background: $panel-warning; }
    &--crit { background: $panel-danger; }
  }

  &__meta {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
  }

  &__pct {
    font-family: $font-primary;
    font-size: 12px;
    font-weight: 700;
    color: $panel-warning;

    &--crit { color: $panel-danger; }
  }

  &__ciudad,
  &__carrier {
    font-family: $font-primary;
    font-size: 11px;
    color: $panel-muted;
  }

  &__carrier {
    background: $panel-surface;
    border: 1px solid $panel-border;
    border-radius: $radius-full;
    padding: 1px 7px;
    font-size: 10px;
  }
}
```

---

### C8 — Persistir threads fijados en localStorage

**Archivo:** `gali-right-panel.component.ts`  
**Tipo:** Modificación de `pinThread` y constructor

```typescript
private readonly PINNED_KEY = 'gali_pinned_threads_v1';

// Reemplazar la inicialización de pinnedThreadIds:
readonly pinnedThreadIds = signal<string[]>(this.loadPinnedFromStorage());

private loadPinnedFromStorage(): string[] {
  try {
    const raw = localStorage.getItem(this.PINNED_KEY);
    return raw ? JSON.parse(raw) : ['gali-main'];
  } catch {
    return ['gali-main'];
  }
}

// Reemplazar el método pinThread existente:
pinThread(id: string): void {
  this.pinnedThreadIds.update(ids => {
    const next = ids.includes(id)
      ? ids.filter(i => i !== id)
      : [...ids, id];
    try { localStorage.setItem(this.PINNED_KEY, JSON.stringify(next)); } catch {}
    return next;
  });
}
```

---

## 4. Archivos Afectados

| Archivo | Tipo de cambio |
|---|---|
| `src/app/pages/gali-v5/components/gali-right-panel/gali-right-panel.component.ts` | Modificación — interfaces + signals + métodos nuevos |
| `src/app/pages/gali-v5/components/gali-right-panel/gali-right-panel.component.html` | Modificación — thread sections + rich cards + context selector |
| `src/app/pages/gali-v5/components/gali-right-panel/gali-right-panel.component.scss` | Modificación — estilos nuevos para sections, rich cards, context selector |
| `src/app/pages/gali-v5/services/gali-state.service.ts` | Modificación — añadir `requestedProjectThread` y `requestedAgentThread` signals |

---

## 5. Orden de Implementación

```
C1 → C8 → C2 → C3 → C4 → C7 → C5 → C6
```

| Paso | Tarea | Por qué en este orden |
|---|---|---|
| **1** | C1 — Ampliar interfaces `ChatThread` y `ChatMessage` | Todo lo demás depende de los tipos correctos |
| **2** | C8 — localStorage en `pinThread` | Cambio mínimo, sin dependencias. Valida que el comportamiento base sigue funcionando |
| **3** | C2 — Secciones en drawer (Fijados/Por Proyecto/Por Agente) | Requiere los computed signals del C1 (`category`) |
| **4** | C3 — Selector de contexto al crear thread | Requiere C2 (el drawer ya tiene la nueva estructura) |
| **5** | C4 — Verificación de historial independiente | Verificación, no código nuevo |
| **6** | C7 — Rich cards en respuestas | Requiere C1 (`richCard?: GaliRichCard` en `ChatMessage`) |
| **7** | C5 — Thread pre-cargado desde proyecto | Requiere C7 (el thread incluye un rich card de acción) |
| **8** | C6 — Thread pre-cargado desde agente | Mismo patrón que C5, se hace después |

**Después de cada tarea:** ejecutar `ng build --configuration development` y verificar 0 errores.

---

## 6. Criterios de Aceptación

### CA-1 — Thread drawer con secciones [C2]
- [ ] El drawer oscuro muestra máximo 4 secciones: FIJADOS, POR PROYECTO, POR AGENTE, SIN CATEGORÍA
- [ ] Una sección solo aparece si tiene threads — no hay headers vacíos
- [ ] Los threads de ejemplo iniciales ("Roax · Collar GPS" y "Vigilante · Novedades") aparecen en "POR PROYECTO" y "POR AGENTE" respectivamente
- [ ] El botón de pin ⊞ es visible al hacer hover sobre cada thread item

### CA-2 — Fijar thread persiste [C8]
- [ ] Hacer pin en cualquier thread → aparece en sección FIJADOS
- [ ] Recargar la página → el thread sigue en FIJADOS (localStorage)
- [ ] Desfijar → sale de FIJADOS y vuelve a su sección original
- [ ] `gali-main` siempre aparece en FIJADOS (no puede desfijarse)

### CA-3 — Selector de contexto [C3]
- [ ] Clic en "+ Nueva conversación" → aparece el selector de 3 opciones dentro del drawer (no navega ni abre modal)
- [ ] Seleccionar "Por proyecto" → aparece la lista de proyectos disponibles
- [ ] Seleccionar "Por agente" → aparece la lista de agentes con su color y descripción
- [ ] Seleccionar "Libre (sin contexto)" → crea el thread inmediatamente sin más pasos
- [ ] Seleccionar un proyecto o agente → crea el thread con título correcto y se activa
- [ ] "Cancelar" → vuelve al botón "+ Nueva conversación"

### CA-4 — Thread con historial independiente [C4]
- [ ] Enviar un mensaje en el thread "Roax · Collar GPS" → aparece solo en ese thread
- [ ] Cambiar al thread "gali-main" → el mensaje anterior NO aparece
- [ ] Cambiar de vuelta → el mensaje sigue ahí

### CA-5 — Rich card ROAS [C7]
- [ ] El thread "roax-collar" muestra el primer mensaje con un mini-gráfico de barras (5 barras L/M/X/J/V)
- [ ] Las barras crecen animadas al primer render (no aparecen de golpe)
- [ ] Las barras con ROAS >= objetivo (2.5×) son verdes; las que no, son naranja suave
- [ ] El valor actual (1.93×) aparece en naranja/ámbar porque está bajo objetivo
- [ ] El texto "Faltan 0.57×" es correcto y visible

### CA-6 — Rich card novedad [C7]
- [ ] El thread "vigilante-novedades" muestra la alerta con barra de calor roja al 18%
- [ ] El porcentaje "18%" aparece en color rojo (>= 15%)
- [ ] Ciudad "Bogotá" y transportadora "Coordinadora" son visibles
- [ ] La barra de calor tiene animación `gali-bar-grow`

### CA-7 — Rich card action buttons [C7]
- [ ] Al crear un nuevo thread de proyecto (C3 → Por proyecto), el primer mensaje de Gali incluye 3 botones de acción
- [ ] Los botones tienen el mismo estilo que `.gali-msg__action-btn`
- [ ] El botón primario ("Ver pedidos") tiene el style `--primary`

### CA-8 — Pre-carga desde proyecto [C5]
- [ ] Llamar `this.gali.requestedProjectThread.set({ projectId: 'collar-gps-2026', projectLabel: 'Collar GPS para Mascotas' })` desde cualquier componente → el panel abre el thread de ese proyecto
- [ ] Si el thread ya existe → se activa sin duplicar
- [ ] El panel cambia a tab "chat" automáticamente

### CA-9 — Pre-carga desde agente [C6]
- [ ] Mismo comportamiento que CA-8 pero con `requestedAgentThread`
- [ ] El mensaje pre-cargado incluye las últimas acciones del agente del `beforeAfterLog`

### CA-10 — Compilación limpia
- [ ] `ng build --configuration development` sin errores TypeScript
- [ ] No existen `data-proto-skip` en botones que deberían ser funcionales (los nuevos CTAs deben funcionar)

---

## 7. Motion Language — Animaciones Específicas de WorkspaceChat

Siguiendo los principios del Motion Language Global (Cambios10jun.md, sección 9.0):

| Elemento | Animación | Timing |
|---|---|---|
| Thread drawer open | `gali-fade-up` (ya existe `threadDrawerIn`) | 180ms, ease-out |
| Thread sections — cada sección | `gali-fade-up` con stagger 50ms por sección | 150ms each |
| Context selector — categorías | `threadDrawerIn` (reutilizar) | 120ms |
| Mensaje con rich card | `execCardIn` (ya existe, reutilizar) | 200ms |
| Barras del ROAS chart | `gali-bar-grow` individual con `animation-delay` 60ms × índice | 300ms each |
| Barra de novedad card | `gali-bar-grow` | 400ms, ease-out |
| Thread item pin (hover reveal) | `opacity: 0 → 1` en el `:hover` del parent | 120ms |
| Nuevo thread creado | Rail actualiza: `gali-fade-up` del nuevo ítem | 200ms |
| `@media (prefers-reduced-motion: reduce)` | `animation-duration: 1ms` en todos los keyframes | — |

---

*Spec 10 — WorkspaceChat · Gali V5 · 2026-06-10 · Cata Giraldo*  
*Depende de: Spec 8 completo. Extiende: gali-right-panel existente. Precede a: Spec 15 (ImpactoGali que usará GaliRichCard para mostrar ROI acumulado).*
