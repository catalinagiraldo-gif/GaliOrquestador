# Spec #3 — Hub Reorganizado: Tres Zonas + ROI de Gali

> **Componente principal**: `home.component` (Gali Hub)  
> **Fecha**: 2026-06-04 · **Última enmienda**: 2026-06-05 (Sprint de cierre)  
> **Estado**: **Pendiente — 0%** (Spec 7 implementó parcialmente bloques de decisiones — integrar, no duplicar)  
> **Prioridad**: P0 — El Hub es la pantalla más importante del producto  
> **Pipeline**: `#3` · Ver `0.Pipeline.md`  
> **Depende de**: Spec 1 ✅ (projects.json) · Spec 2 (navegación estable)  
> **Desbloquea**: Spec 4 (modal confirmación fase 2) · Spec 5 (toggle en Zona 1)  
> **Fuentes**: CorrecionesGali5.md (Bloque 1 #2 #4 #5 #8 #11 #12 · Bloque 2 #1 #4 #8 · Heurísticas 3 y 8) · DesADIGali5.md (§1 tabla de inconsistencias) · GaliAjustesJun3.md (Bloque 6) · mocks/gali-v5/projects.json  
> **Skills requeridas**: `/bencium-impact-designer` + `/interface-design`

---

## 1. Diagnóstico — Por qué el Hub actual no funciona

### 1.1 Sobrecarga cognitiva (Heurística #8)

El Hub acumula **7 bloques de contenido** compitiendo simultáneamente sin jerarquía visual clara:
barra de intent global · tabs de dashboard · banner de objetivo · banner de alerta de agente · ciclo de negocio (6 tarjetas) · Decisiones pendientes (cards con acciones) · chat de Gali con sugerencias · Akademy recomendado.

El usuario no sabe qué atender primero. La heurística de minimalismo exige reducir la densidad sin perder funcionalidad.

### 1.2 Acciones masivas sin confirmación (Heurística #3)

Los botones "Cambiar todos → Servientrega" y "Cambiar solo los de hoy" en Decisiones pendientes ejecutan acciones sobre pedidos activos sin ningún paso de confirmación. Redirigir 12+ pedidos de transportadora en tiempo real tiene consecuencias financieras directas. Un clic accidental es crítico e irreversible.

### 1.3 Ciclo de negocio — layout roto (Problema #2)

Las 6 tarjetas del ciclo de negocio (Producto → Marketing → Pedidos → Logística → Finanzas → Reportes) no fluyen linealmente:
- Logística aparece desfasada hacia abajo y a la derecha
- No hay flechas conectoras entre todas las tarjetas
- Hay dos columnas completamente vacías sin estado explícito — el usuario no sabe si es un error o contenido pendiente

### 1.4 Dos puntos de entrada al chat de Gali (Problema #5 + "Reformular Hub")

La barra de intent superior (`"¿Qué quieres hacer?"`) y el panel lateral `"Hablar con Gali"` hacen exactamente lo mismo. La redundancia confunde sobre cuál usar y ocupa espacio visual valioso que podría usarse para contenido de mayor urgencia.

### 1.5 ROAS incorrecto en tarjeta de Marketing

La tarjeta de Marketing del Hub muestra `2.9x` como ROAS — ese es el valor **declarado por Meta Ads**, no el ROAS real de Dropi. El valor real según el mock maestro (`projects.json`) es `1.93x`. Mostrar el dato de Meta como si fuera el real distorsiona la lectura del negocio.

### 1.6 "Ciclo de negocio" como micro-label invisible (Problema #12)

La etiqueta `"↻ Ciclo de negocio"` aparece como texto muy pequeño junto al header. No guía al usuario sobre cómo leer el flujo central de la pantalla.

### 1.7 Sin ROI acumulado de Gali (Bloque 2 #1 + #8)

No existe ningún indicador que muestre qué hizo Gali automáticamente vs. qué requiere decisión del usuario. El Hub se percibe como un dashboard tradicional de KPIs, no como un orquestador IA que ejecuta acciones por el usuario. Sin ese número de valor percibido, el dropshipper no puede justificar emocionalmente que Gali es indispensable.

---

## 2. GenUI Progresiva — El Hub en 3 estados (Bloque 6 de GaliAjustesJun3)

El Hub no debe tener una densidad fija. Su layout se revela progresivamente según el nivel del usuario:

| Estado | Perfil | Densidad del Hub |
|---|---|---|
| **Estado Cero** | Usuario nuevo, sin proyectos activos | 3 módulos visibles máximo · Onboarding conversacional · Sin ciclo de negocio (vacío) |
| **Estado Activo** | Usuario con 1-3 proyectos en curso | Las 3 zonas de esta spec · Alertas consolidadas · Ciclo de negocio con datos reales |
| **Estado Experto** | Operador con 3+ proyectos, múltiples agentes | Todas las zonas + toggle "Vista experto" que expande sub-métricas · Gali propone layout óptimo |

**Esta spec define el layout para Estado Activo** — que es el estado del usuario demostrado en el prototipo (47 pedidos/sem, 2+ proyectos).

El botón `"← Vista simple"` existente pasa a ser el toggle entre Estado Activo y Estado Cero. `"↺ Reconfigurar Gali"` abre un modal de configuración del workspace — no resetea todo el sistema.

---

## 3. Nueva Estructura del Hub — Tres Zonas

> **⚠ Sprint de cierre (2026-06-05)**: el DOM debe renderizar Zona 1 **siempre antes** que Zona 2. Esto afecta el orden de los bloques en `home.component.html` — no son intercambiables con CSS `order`. Ver §11 paso 1.

```
┌──────────────────────────────────────────────────────────────────────┐
│  BARRA DE INTENT GLOBAL (se mantiene, única entrada al chat de Gali) │
├──────────────────────────────────────────────────────────────────────┤
│  ZONA 1 — DECISIONES PENDIENTES + SEÑALES DE AGENTES                │
│  Mayor peso visual · Acción requerida del usuario                    │
│  [Banner "Gali esta semana" — colapsado si hay decisiones pendientes]│
├──────────────────────────────────────────────────────────────────────┤
│  ZONA 2 — CICLO DE NEGOCIO (título visible)                          │
│  Producto → Marketing → Pedidos → Logística → Finanzas → Reportes   │
│  Flechas conectoras entre todas · Estado explícito en celdas vacías  │
├──────────────────────────────────────────────────────────────────────┤
│  ZONA 3 — GALI ESTA SEMANA + AKADEMY (pill)                          │
│  Colapsada por defecto · Chat como drawer lateral desde header       │
└──────────────────────────────────────────────────────────────────────┘
```

### Separación visual entre zonas

Cada zona debe ser visualmente distinguible sin necesidad de leer los títulos:
- Zona 1: borde izquierdo naranja `2px solid var(--primary-500)` + fondo `var(--surface-50)` (off-white muy sutil, no blanco puro)
- Zona 2: fondo blanco, título de sección prominente (ver §5)
- Zona 3: fondo `var(--surface-100)`, menor altura visual, ligeramente más oscura

---

## 4. Zona 1 — Decisiones Pendientes + Señales de Agentes

### 4.1 Layout de Zona 1

```
┌────────────────────────────────────────────────────────────┐
│  🔴 Decisiones pendientes (2)          [Agentes activos]   │
│  ──────────────────────────────────────────────────────    │
│  [card 1]  Coordinadora Bogotá: 15% novedad hoy            │
│            Vigilante recomienda cambio de transportadora   │
│            [Cambiar todos →]  [Solo los de hoy]            │
│                                                            │
│  [card 2]  ROAS real 1.93x                                 │
│            Meta declarada: 2.9x                            │
│            Roax: escalar pauta $66k → $86k/día             │
│            [Aprobar escala]   [Ver análisis]               │
└────────────────────────────────────────────────────────────┘
```

- El título "Decisiones pendientes (N)" tiene un badge con el conteo de decisiones activas
- Los agentes activos se muestran como pills compactos a la derecha: `● Vigilante activo` · `● Roax activo` · `● Kronos activo`
- Las cards de decisión tienen jerarquía visual por urgencia: borde izquierdo `var(--red-400)` para crítico, `var(--amber-400)` para recomendación

> **Sprint de cierre — card 2 (confirmScale)**: el encabezado visual de la card de escala debe mostrar **"ROAS real 1.93x"** como valor primario (Inter Bold 18px, `var(--gray-900)`) y **"Meta declarada: 2.9x"** como sublabel secundario (Inter Regular 10px, `var(--gray-400)`). El ratio de Meta nunca debe aparecer como el número principal. La card usa la misma jerarquía tipográfica que §5.3.

> **Sprint de cierre — CTAs de `gali-signal-card-v2`**: los botones de acción masiva en cards del tipo `gali-signal-card-v2` deben estar conectados a los handlers del componente padre:
> - `[Cambiar todos →]` / `[Solo los de hoy]` → `showBulkModal(signal)` (abre modal 2 pasos de §4.2)
> - `[Aprobar escala]` → `openScaleConfirm(signal)` (abre modal con `confirmScale: true`)
> - `[Ver análisis]` → navegación a detalle del agente (puede ser `data-proto-skip` por ahora)
>
> En `home.component.html`, las cards deben emitir eventos `(onBulkAction)` y `(onScaleConfirm)` que el Hub escucha. No llamar funciones directamente desde la card — mantener el patrón de output/input existente de Spec 7.

### 4.2 Modal de confirmación de 2 pasos (Heurística #3)

**Todo botón de acción masiva en Zona 1 abre un modal antes de ejecutar.**

El modal tiene 2 pasos:

**Paso 1 — Contexto de la acción**:
```
┌─────────────────────────────────────────────────────┐
│  Cambiar transportadora en pedidos activos          │
│  ─────────────────────────────────────────────────  │
│  Vigilante detectó: Coordinadora Bogotá 15% novedad │
│                                                     │
│  Esta acción afecta:                                │
│  • 12 pedidos activos con destino Bogotá            │
│  • Transportadora destino: Servientrega             │
│  • Impacto estimado: evitar ~3 novedades ($51k)     │
│  • Tiempo de gestión: ~2 horas adicionales          │
│                                                     │
│  [Cancelar]                    [Revisar y confirmar →]│
└─────────────────────────────────────────────────────┘
```

**Paso 2 — Confirmación final**:
```
┌─────────────────────────────────────────────────────┐
│  Confirmar cambio                                   │
│  ─────────────────────────────────────────────────  │
│  ✓ 12 pedidos · Bogotá · → Servientrega             │
│                                                     │
│  Esta acción no se puede deshacer.                  │
│  Vigilante notificará cuando esté completo.         │
│                                                     │
│  [← Volver]              [Confirmar cambio ahora]   │
└─────────────────────────────────────────────────────┘
```

**Specs del modal**:
- Overlay con `backdrop: blur(4px) rgba(0,0,0,0.3)`
- Ancho máx: 480px, centrado vertical y horizontalmente
- Botón de confirmación: color de fondo `var(--primary-500)` (#f49a3d), texto blanco
- Botón destructivo (si aplica): borde `var(--red-500)`, texto rojo, fondo blanco
- El modal sigue las reglas de `dropi-modal.json` del DS Registry

**Acciones que SIEMPRE requieren este modal**:
- Cualquier cambio que afecte más de 1 pedido
- Escalar o pausar pauta publicitaria
- Cambiar transportadora en lote

### 4.3 Bloque "Gali esta semana" en Zona 1

Justo debajo de las cards de Decisiones, antes de Zona 2, un bloque compacto de 3 líneas que muestra el ROI acumulado de Gali:

```
┌────────────────────────────────────────────────────────┐
│  ✦ Gali esta semana                          [▾ / ▴]   │
│  ──────────────────────────────────────────────────    │
│  ⚡ 23 acciones ejecutadas automáticamente             │
│  💰 $85k ahorrados en novedades evitadas              │
│  🔭 2 oportunidades de escala detectadas              │
└────────────────────────────────────────────────────────┘
```

**Datos derivados del mock** (`projects.json`):
- Acciones ejecutadas: suma de eventos de agentes activos (`vigilante`: 12 pedidos redirigidos + 7 novedades resueltas · `roax`: 3 campañas ajustadas · `chatea`: 1 escalación) = `23 acciones`
- Ahorro estimado: `$85k` (basado en tarjeta del Hub actual, derivado de tasa_novedad_pct 15% × pedidos 47 × CPA $21.4k × factor estimado)
- Oportunidades detectadas: 2 proyectos con señal de escala (collar-gps `pauta_escala_propuesta: $86k` + skincare-kbeauty `pauta_escala_propuesta: $40k`)

**Specs visuales**:
- Fondo: `var(--orange-50)` — muy sutil, para diferenciarlo de las cards de decisión
- Ícono `✦` Gali: color `var(--primary-500)` #f49a3d
- Texto de líneas: Inter Regular 13px, `var(--gray-700)`
- Valores numéricos: Inter SemiBold 13px, `var(--gray-900)`
- El bloque tiene un link `"Ver historial completo →"` que en esta iteración puede ser `data-proto-skip` con tooltip "próximamente"

> **Sprint de cierre — densidad adaptativa**: el bloque "Gali esta semana" es un **insight banner colapsable**. Su comportamiento depende del estado de Zona 1:
>
> | Estado de Zona 1 | Comportamiento del insight banner |
> |---|---|
> | Hay decisiones pendientes (`pendingApprovalSignals.length > 0`) | Banner **colapsado por defecto** — muestra solo el header `"✦ Gali esta semana [▾]"` en 40px. El usuario puede expandirlo manualmente. |
> | Sin decisiones pendientes (Zona 1 vacía) | Banner **expandido por defecto** — las 3 líneas de métricas son visibles. |
>
> **Razón**: cuando hay decisiones urgentes, el insight de ROI es información secundaria que no debe competir visualmente con la acción requerida. La densidad se reduce automáticamente.
>
> **Implementación**:
> ```typescript
> // home.component.ts
> get insightBannerCollapsed(): boolean {
>   return this.pendingApprovalSignals.length > 0;
> }
> ```
> ```html
> <!-- home.component.html -->
> <div class="insight-banner" [class.is-collapsed]="insightBannerCollapsed">
>   <div class="insight-header" (click)="toggleInsightBanner()">
>     <span>✦ Gali esta semana</span>
>     <i class="pi" [class.pi-chevron-down]="insightBannerCollapsed"
>                    [class.pi-chevron-up]="!insightBannerCollapsed"></i>
>   </div>
>   <div class="insight-body"><!-- 3 líneas de métricas --></div>
> </div>
> ```
> Transición: `max-height` de `40px` → `120px`, 200ms ease-out (mismo patrón que Zona 3).

---

## 5. Zona 2 — Ciclo de Negocio (rediseño)

### 5.1 "Ciclo de negocio" como título de sección visible

El micro-label `"↻ Ciclo de negocio"` se convierte en un **título H2 de sección** prominente:

```html
<div class="section-header">
  <h2 class="section-title">Ciclo de negocio</h2>
  <span class="section-subtitle">
    Flujo de tu operación esta semana
  </span>
  <button class="section-info-btn" pTooltip="Cómo leer este flujo" tooltipPosition="right">
    <i class="pi pi-info-circle"></i>
  </button>
</div>
```

**Specs tipográficas** (DS tokens):
- `h2.section-title`: Inter SemiBold 16px, `var(--gray-900)`
- `span.section-subtitle`: Inter Regular 12px, `var(--gray-500)`
- Separación del header al grid de tarjetas: `$size-4` (16px)

### 5.2 Layout horizontal con flechas conectoras

Las 6 tarjetas deben organizarse en **una fila horizontal** con flechas entre todas:

```
[Producto] → [Marketing] → [Pedidos] → [Logística] → [Finanzas] → [Reportes]
```

- Si el viewport no da el ancho, el contenedor tiene `overflow-x: auto` con `scroll-snap-type: x mandatory`
- Las flechas conectoras son SVG inline con `var(--gray-300)`, 12px de alto, posicionadas en el gap entre tarjetas

**Solución para las dos columnas vacías**:

Las celdas vacías (actualmente sin estado) deben mostrar un estado explícito:

```html
<!-- tarjeta en estado vacío -->
<div class="cycle-card cycle-card--empty">
  <div class="empty-icon">
    <img src="assets/icons/sidebar/gali-v5/boxes.svg" width="20" height="20" />
  </div>
  <p class="empty-label">Sin datos aún</p>
  <p class="empty-cta">Conecta tu catálogo</p>
</div>
```

- Fondo: `var(--surface-100)` con borde dashed `var(--gray-200)`
- Texto vacío: Inter Regular 12px, `var(--gray-400)`
- Sin estado de error — es un estado neutral de "aún no conectado"

### 5.3 ROAS correcto en tarjeta Marketing

**Antes** (incorrecto): `ROAS 2.9x`

**Después** (correcto — del mock):
```html
<div class="cycle-metric">
  <span class="metric-value">1.93x</span>
  <span class="metric-label">ROAS real</span>
  <span class="metric-sublabel">Meta declarada: 2.9x</span>
</div>
```

**Specs**:
- `metric-value`: Inter Bold 18px, `var(--gray-900)`
- `metric-label`: Inter Medium 11px, `var(--gray-600)`
- `metric-sublabel`: Inter Regular 10px, `var(--gray-400)` — este es el dato de Meta, en tono secundario

**Valores correctos por tarjeta del ciclo** (del mock `collar-gps-2026` como proyecto principal):

| Tarjeta | Métrica principal | Valor (mock) | Sublabel | Agente en nodo |
|---|---|---|---|---|
| Producto | Pedidos activos | `47/sem` | Estado: en_escala | — |
| Marketing | ROAS real | `1.93x` | Meta declarada: 2.9x | Roax (`var(--amber-400)`) |
| Pedidos | Pedidos / sem | `47` | 12 en riesgo logístico | Vigilante (`var(--red-400)`) |
| Logística | Tasa novedad | `15%` | Coordinadora Bogotá | Vigilante (`var(--red-400)`) |
| Finanzas | Ganancia neta | `$411k/sem` | Margen 22% | **Kronos** (`#60a5fa`) |
| Reportes | Revenue | `$1.82M/sem` | +$89k vs sem anterior | — |

> **Sprint de cierre — tarjeta Finanzas**: esta tarjeta tiene contenido más denso que las demás por la presencia del agente Kronos y la alerta fiscal. El layout de la tarjeta debe ser:
>
> ```
> ┌─────────────────────────────────────────────┐
> │  Finanzas                    ● Kronos activo │
> │  ─────────────────────────────────────────  │
> │  $411k/sem         Margen 22%               │
> │                                             │
> │  28 facturas · $450k · 6 días               │
> │  ⚠ Riesgo fiscal                            │
> │                                             │
> │  [Conectar Siigo →]                         │
> └─────────────────────────────────────────────┘
> ```
>
> **Specs del nodo Finanzas**:
> - Pill del agente Kronos: `● Kronos activo` — punto de color `#60a5fa` (azul Kronos, diferencia a Kronos del resto de agentes naranjas/rojos)
> - Fila de datos fiscales: `28 facturas · $450k · 6 días` — Inter Regular 11px, `var(--gray-600)`
> - Alerta `⚠ Riesgo fiscal`: Inter Medium 11px, color `var(--amber-600)`, fondo `var(--amber-50)`, border-radius `$radius-1`, padding `2px 6px`
> - CTA `[Conectar Siigo →]`: botón estilo "link" (text-only, no background), Inter Medium 12px, color `#60a5fa` — el color es el de Kronos para señalar que es Kronos quien ejecutará la acción
> - La alerta y el CTA SOLO se muestran si `kronos_riesgo_fiscal: true` en el mock (ver §7)
>
> **Razón del color `#60a5fa` para Kronos**: cada agente tiene un color identificador propio en el nodo del ciclo. Kronos opera en el espacio financiero y usa azul (`#60a5fa`) para distinguirse de Vigilante (rojo) y Roax (ámbar). El naranja primario de Dropi nunca se usa para identificar agentes — es el color de la plataforma.

### 5.4 Agentes en nodos del Ciclo

Cada tarjeta del ciclo puede tener un agente activo indicado como pill en la esquina superior derecha:

```html
<!-- ejemplo: nodo Finanzas con Kronos -->
<div class="cycle-card cycle-card--has-agent">
  <div class="cycle-card__agent-pill" style="--agent-color: #60a5fa">
    <span class="agent-dot"></span>
    <span class="agent-name">Kronos</span>
  </div>
  <!-- contenido de la tarjeta -->
</div>
```

```scss
.cycle-card__agent-pill {
  display: flex;
  align-items: center;
  gap: 4px;
  font-size: 10px;
  font-family: Inter, sans-serif;
  font-weight: 500;
  color: var(--agent-color);

  .agent-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--agent-color);
  }
}
```

**Mapa de agentes por nodo**:

| Nodo | Agente | Color |
|---|---|---|
| Marketing | Roax | `var(--amber-400)` = `#f59e0b` |
| Pedidos | Vigilante | `var(--red-400)` = `#f87171` |
| Logística | Vigilante | `var(--red-400)` = `#f87171` |
| Finanzas | Kronos | `#60a5fa` |
| Producto | — | (sin agente) |
| Reportes | — | (sin agente, puede expandirse) |

---

## 6. Zona 3 — Gali Esta Semana + Akademy (colapsada)

### 6.1 Zona 3 colapsada por defecto

La Zona 3 arranca contraída y muestra solo un handle visual:

```
──────────────────────────────────────────────────────────
  ▸ Academy · Personalizado para ti · [1 curso nuevo]
──────────────────────────────────────────────────────────
```

Al expandir, muestra:
- Un pill de "Akademy" con el curso recomendado por Gali (enlace a `/gali-v5/academy`)
- El bloque "Gali esta semana" se mueve aquí **solo si la Zona 1 ya está vacía** (sin decisiones pendientes) — de lo contrario permanece en Zona 1

**Transición de colapso**: `max-height` de `40px` → `180px`, 200ms ease-out.

### 6.2 Chat de Gali — eliminar redundancia (Problema #5)

**Problema actual**: La barra de intent superior y el panel `"Hablar con Gali"` son el mismo punto de entrada.

**Solución**:
- La **barra de intent superior** (`"¿Qué quieres hacer?"`) se mantiene como el **único** punto de entrada al chat de Gali
- El panel lateral `"Hablar con Gali"` se convierte en un **drawer** que se abre desde el header (botón ícono de Gali, ya existente)
- El drawer se desliza desde la derecha: `transform: translateX(100%)` → `translateX(0)`, 250ms ease-in-out
- El drawer tiene ancho fijo 360px y `position: fixed` con `z-index: 1000`
- La barra de intent en el Hub puede tener un CTA secundario `"Abrir chat completo →"` que abre el drawer

**Archivos afectados**:
- `home.component.html`: eliminar el bloque del panel `"Hablar con Gali"` del layout del Hub
- El componente del drawer (si ya existe como `gali-right-panel`) se activa solo desde el header, no desde el Hub

---

## 7. Datos del Mock a usar en el Hub

Todos los valores dinámicos del Hub deben derivarse de `mocks/gali-v5/projects.json`. El proyecto activo por defecto en el Hub es `collar-gps-2026`.

| Campo en Hub | Campo en mock | Valor |
|---|---|---|
| ROAS real (Marketing) | `roas_real` | `1.93x` |
| ROAS Meta (sublabel) | `roas_meta` | `2.9x` |
| Pedidos activos | `pedidos_sem` | `47` |
| Ganancia neta | `ganancia_neta_semanal_label` | `$411k` |
| Revenue semanal | `revenue_semanal_label` | `$1.82M` |
| Pauta actual | `pauta_diaria_label` | `$66k/día` |
| Pauta propuesta (escala) | `pauta_escala_propuesta_label` | `$86k/día` |
| Tasa novedad | `tasa_novedad_label` | `15%` |
| Ciudad novedad | `tasa_novedad_ciudad` | `Coordinadora Bogotá` |
| Delta semana | `semana_delta` | `+$89k vs sem anterior` |
| Margen | `margen_neto_pct_label` | `22%` |
| Pedidos en riesgo (decisión) | — | `12` (constante de signals) |
| Ahorro Gali semana | — | `$85k` (constante del hub) |
| Acciones ejecutadas | — | `23` (suma de agentes activos) |
| Oportunidades detectadas | — | `2` (proyectos con pauta_escala_propuesta activa) |
| Facturas pendientes (Finanzas) | `kronos_facturas_pendientes` | `28` |
| Valor facturas pendientes | `kronos_facturas_valor_label` | `$450k` |
| Días promedio cobro | `kronos_dias_cobro` | `6 días` |
| Alerta fiscal activa | `kronos_riesgo_fiscal` | `true` |

> **Nota sobre campos Kronos**: si `kronos_riesgo_fiscal` no existe en el mock actual, agregar los campos al objeto `collar-gps-2026` en `projects.json`. Son los únicos campos que sí se pueden añadir al mock — la restricción de §8 es "no modificar sin autorización", pero agregar campos nuevos para Kronos está autorizado por esta spec.

---

## 8. Archivos afectados

| Archivo | Tipo de cambio |
|---|---|
| `home.component.html` | Restructurar en 3 zonas con DOM Zona 1 antes de Zona 2 · Eliminar panel "Hablar con Gali" · Modal de confirmación · H2 visible · Flechas entre tarjetas · Estado vacío en celdas · Pill Kronos en nodo Finanzas · Banner insight colapsable |
| `home.component.ts` | Leer datos de `projects.json` · Handler modal 2 pasos · `showBulkModal()` · `openScaleConfirm()` · `toggleInsightBanner()` · `insightBannerCollapsed` getter · Toggle zona 3 · Drawer de chat |
| `home.component.scss` | Layout 3 zonas · Modal · Ciclo horizontal scroll · Estado vacío · Animación Zona 3 · `.cycle-card__agent-pill` con `--agent-color` CSS var · `.insight-banner.is-collapsed` · Alerta `⚠ Riesgo fiscal` |
| `mocks/gali-v5/projects.json` | **Agregar** campos Kronos: `kronos_facturas_pendientes`, `kronos_facturas_valor_label`, `kronos_dias_cobro`, `kronos_riesgo_fiscal` en objeto `collar-gps-2026` |
| `gali-signal-card-v2` (si existe) | Verificar que emite `(onBulkAction)` y `(onScaleConfirm)` — no hardcodear llamadas a funciones del Hub |
| Componente drawer de Gali (si existe) | Activar desde header en lugar del Hub |

---

## 9. DS Rules — Checklist

Antes de implementar, leer según la tabla de CLAUDE.md:

- [ ] `dropi-modal.json` — para el modal de confirmación de 2 pasos
- [ ] `dropi-button.json` — botones de acción en Decisiones pendientes ("Aprobar", "Cancelar", "Confirmar")
- [ ] `dropi-card.json` — tarjetas del ciclo de negocio y cards de Decisiones
- [ ] `dropi-badge.json` — badge de conteo en "Decisiones pendientes (N)"
- [ ] `tokens/colors.json` — verificar `--primary-500`, `--gray-*`, `--surface-*`, `--red-400`, `--amber-400`
- [ ] `tokens/spacing.json` — gaps entre zonas, padding interno de cards
- [ ] `tokens/typography.json` — Inter SemiBold 16px para títulos de sección

**Recordatorio de reglas no-negotiable**:
- Primary color: naranja `#f49a3d` — no verde, no azul
- Font: Inter (cuerpo), IBM Plex Sans (menú), Montserrat (solo avatares)
- Íconos: `<img>` tags 20×20px, NO PrimeIcons `<i>` para iconos de acción
- Nunca hardcodear valores que existen como DS tokens

---

## 10. Lo que NO cambia

- Las rutas y navegación a módulos individuales (Logística, Reportes, etc.) — el Hub no reemplaza el acceso, solo organiza los datos
- El comportamiento del workspace mode bar (Operar/Lanzar/Medir) — cubierto en Spec #2
- Los tabs del Hub (Principal/Mi negocio) — cubierto en Spec #2
- El sistema de pestañas de proyecto y el "+" — cubierto en Spec #2

---

## 11. Orden de implementación

> **Regla de DOM obligatoria**: en `home.component.html`, el bloque de Zona 1 DEBE aparecer antes del bloque de Zona 2 en el HTML fuente. No usar CSS `order` para reordenar — el orden semántico importa para accesibilidad y para que los handlers de Zona 1 se registren antes.

1. **`mocks/gali-v5/projects.json`** — agregar campos Kronos al objeto `collar-gps-2026` antes de cualquier código
2. **`home.component.scss`** — definir las 3 zonas con separación visual; agregar `.cycle-card__agent-pill`, `.insight-banner.is-collapsed`, estilos de alerta fiscal
3. **`home.component.ts`** — conectar mock `projects.json`, crear handlers: `showBulkModal()`, `openScaleConfirm()`, `toggleInsightBanner()`, getter `insightBannerCollapsed`, handler modal 2 pasos
4. **`home.component.html`** — restructurar en 3 zonas (Zona 1 primero en DOM), ciclo horizontal, título H2 visible, ROAS 1.93x como valor primario en card de escala, estado vacío, bloque insight banner con lógica de colapso
5. **Nodo Finanzas** — pill Kronos `#60a5fa`, datos `28 facturas · $450k · 6 días`, alerta `⚠ Riesgo fiscal`, CTA Siigo
6. **CTAs `gali-signal-card-v2`** — verificar outputs `(onBulkAction)` / `(onScaleConfirm)` y enlazarlos a `showBulkModal` / `openScaleConfirm` en el Hub
7. **Modal de confirmación** — overlay inline, 2 pasos, reglas de §4.2
8. **Eliminar panel "Hablar con Gali"** del layout del Hub
9. **Zona 3** — colapso por defecto, pill de Akademy
10. Verificar compilación sin errores
11. Smoke test visual: 3 zonas claramente diferenciadas · ROAS 1.93x visible con "Meta 2.9x" como sublabel · modal abre al pulsar "Cambiar todos" · Kronos azul en nodo Finanzas · banner insight colapsado cuando hay decisiones · banner expandido cuando Zona 1 está vacía

---

## 12. Integración con Spec 7 (ya parcialmente en código)

Spec 7 implementó antes de Spec 3:
- Bloques `mission__signals-block--done` / `--pending` ("Gali ya lo hizo" / "Necesitas decidir")
- Botón "⚙ Umbrales" + `GaliAgencyThresholdsPanelComponent`
- Botón footer "← Modo básico"

**Instrucción**: al reorganizar Zona 1, **reutilizar** estos bloques dentro de la nueva estructura de zonas. No eliminar ni reimplementar la lógica de `completedSignals` / `pendingApprovalSignals`.

---

## 13. Criterios de aceptación

- [ ] **CA-1**: Hub dividido en **3 zonas** visualmente distinguibles (borde naranja Zona 1, blanco Zona 2, surface-100 Zona 3).
- [ ] **CA-2**: Zona 1 — Decisiones pendientes con badge de conteo; cards con borde por urgencia.
- [ ] **CA-3**: Modal **2 pasos** abre al pulsar acciones masivas ("Cambiar todos →", "Aprobar escala").
- [ ] **CA-4**: Bloque **"Gali esta semana"** con 3 líneas (acciones, ahorro, oportunidades) derivadas de mock.
- [ ] **CA-5**: Ciclo de negocio — **fila horizontal** con flechas; título H2 visible; celdas vacías con estado "Sin datos aún".
- [ ] **CA-6**: Tarjeta Marketing muestra **ROAS real 1.93x** como valor primario + sublabel "Meta declarada: 2.9x" (desde `projects.json`); nunca al revés.
- [ ] **CA-7**: Panel lateral "Hablar con Gali" **eliminado** del layout Hub; barra intent superior es única entrada.
- [ ] **CA-8**: Zona 3 colapsada por default con pill Academy.
- [ ] **CA-9**: Bloques Spec 7 integrados en Zona 1 sin regresión.
- [ ] **CA-10**: `ng build` sin errores.
- [ ] **CA-11**: DOM renderiza Zona 1 **antes** que Zona 2 en el HTML fuente (verificar DevTools → Elements).
- [ ] **CA-12**: Nodo Finanzas muestra pill `● Kronos` en `#60a5fa` + datos `28 facturas · $450k · 6 días` + alerta `⚠ Riesgo fiscal` + CTA `[Conectar Siigo →]`.
- [ ] **CA-13**: Card de confirmScale en Zona 1 muestra `ROAS real 1.93x` como header y `Meta declarada: 2.9x` como sublabel secundario.
- [ ] **CA-14**: Banner "Gali esta semana" está **colapsado** cuando `pendingApprovalSignals.length > 0`; **expandido** cuando Zona 1 está vacía.
- [ ] **CA-15**: Botones masivos de `gali-signal-card-v2` disparan `showBulkModal()` y `openScaleConfirm()` en el Hub (no en la card).
- [ ] **CA-16**: El botón "+1 más…" en las cards de Decisiones pendientes está reemplazado por "Ver más opciones (N)" donde N es el número real de opciones adicionales. Si las opciones son ≤ 3, se muestran directamente en el card sin botón adicional.
- [ ] **CA-17**: El FAB naranja "+" del Hub muestra un mini-menú al hacer clic con al menos 2 opciones rápidas ("Nuevo proyecto", "Nueva skill"). El menú se cierra al hacer clic fuera. Las opciones pueden tener `data-proto-skip` pero el menú debe renderizarse.
- [ ] **CA-18**: El subtítulo genérico "Lanzar · Explorar · Decidir" dentro del panel o área del chat Gali está reemplazado por copy descriptivo de las capacidades o por 3 chips que activan el modo correspondiente del workspace mode bar.
- [ ] **CA-19**: En el header de Zona 2 (ciclo de negocio), hay un widget de proyección colapsable que muestra: "Con las recomendaciones actuales → +X pedidos/sem en 30 días". El valor X se deriva de `projects.json` (diferencia entre `pauta_escala_propuesta` y `pauta_diaria`).

### Tareas adicionales (brechas post-ejecución)

- **C16**: Localizar el botón "+1 más…" (o texto similar) en las cards de `pendingApprovalSignals` en Zona 1. Reemplazarlo por texto dinámico `"Ver más opciones ({{ opciones.length - 2 }})"` o expandir las opciones directamente si son ≤ 3.
- **C17**: En el FAB naranja "+" del Hub (`home.component.html`), añadir lógica de mini-menú: `showFabMenu = signal(false)`. Al hacer clic, muestra un `<div class="fab-menu">` con opciones. Al hacer clic fuera del menú (`@HostListener('document:click')`), cierra el menú.
- **C18**: Buscar en `home.component.html` el subtítulo "Lanzar · Explorar · Decidir" y reemplazarlo. Opción A: 3 `<button class="mode-chip">Lanzar</button>` que llamen `ws.setMode('lanzar')`. Opción B: texto descriptivo "Gali te ayuda a lanzar productos, explorar oportunidades y tomar decisiones."
- **C19**: En la sección de Zona 2 del Hub (ciclo de negocio), añadir un widget de proyección con clase `growth-projection-banner`. Calcularlo: `proyeccion_delta_pedidos_sem` = valor en `projects.json` (puede ser un campo estático en el proyecto activo). Si el campo no existe en projects.json, añadirlo como `"proyeccion_delta_pedidos_sem": 18`.

**Go Agent mode**: ⛔ Requiere Spec 1 cierre + Spec 2 CA-1 a CA-8 en ✅.
