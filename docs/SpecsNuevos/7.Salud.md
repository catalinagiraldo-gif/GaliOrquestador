# Spec 7 — Score de Salud, Tasa de Éxito y Umbrales de Agencia

**Versión**: 1.1  
**Fecha**: Junio 4, 2026  
**Estado**: **Hecho — ~95%** (verificar en código; deuda documentada en backlog)  
**Prioridad**: P2 — Puede ejecutarse en paralelo tras Spec 1  
**Pipeline**: `#7` · Ver `0.Pipeline.md`  
**Depende de**: Spec 1 (blanda) · Spec 3 (refactor Zona 1 — integrar bloques, no rehacer)  
**Superficies**: `dropi-header-ia2` · `agentes-page` · `home (Hub)` · `gali-agency-thresholds-panel`  
**Fuentes**: CorrecionesGali5.md (heurísticas #9, #10, #11 · Bloque 2 #8, #9 · Heurísticas 3 y 7) · DesADIGali5.md (sección 6) · GaliAjustesJun3.md (Bloque 3 tabla de agentes · Bloque 8 límites)  
**Skills requeridas**: `/interface-design`

---

## Problema original

Cuatro problemas de usabilidad críticos sin resolver:

1. **Score 78 sin escala visible ni acción directa** — el usuario veía un número con un tooltip de 4 métricas pero sin peso de cada factor, sin benchmark comparativo y sin una ruta clara para mejorar el número (Corrección #9).

2. **Tasa de éxito de agentes sin definición ni semáforo** — "79% de éxito" en ADA Spy no comunica nada si el usuario no sabe qué mide ese porcentaje ni si 79% es bueno o malo (Corrección #10).

3. **Umbrales de autonomía no configurables desde una sola pantalla** — "Capa Determinista" aparecía como texto informativo al pie de la página de Agentes. Imposible acceder desde el Hub (Bloque 2 #9).

4. **"Decisiones pendientes" mezcla lo que Gali ya hizo con lo que el usuario debe aprobar** — el dropshipper no sabe qué ya está resuelto (Bloque 2 #8), contradiciendo la promesa de libertad operativa.

5. **"← Vista simple" sin contexto** — el usuario no sabe qué información pierde al activarla (Corrección #11).

---

## Tareas

### Hecho ✅

## Cambios implementados

### 1. Score de Salud 78 — panel lateral completo

**Antes**: click → pequeño tooltip/dropdown de 4 métricas sin escala.  
**Después**: click → panel lateral `health-panel` con:

- **SVG ring progress** (circumference basado en r=26 → 163.4px, fill proporcional al score)
- **Score prominente**: 78/100 · "Negocio saludable" (≥70 = verde, ≥40 = ámbar, <40 = rojo)
- **Benchmark Dropi**: 82/100 · "Estás 4 pts por debajo" (delta calculado dinámicamente)
- **4 componentes con peso visual**:

| Componente | Peso | Score | Estado |
|---|---|---|---|
| ROAS promedio 7 días | 30% | 87 | ✓ |
| Tasa de novedades | 25% | 70 | ⚠ |
| Conversión | 20% | 80 | ✓ |
| P&L vs objetivo | 25% | 75 | ✓ |

  Cada componente tiene: barra de progreso horizontal + marcador vertical del promedio Dropi (82%)

- **Recomendaciones priorizadas de Gali**:
  1. Reducir novedades al 10% → +5 pts → [Ver smart routing →]
  2. Cerrar gap ROAS Meta vs real → +3 pts → [Ver análisis Roax →]

**Cierre del panel**: backdrop click o botón ×. Los CTAs navegan y cierran el panel.

---

### 2. Tasa de éxito de agentes — semáforo + benchmark

**Antes**: "79%" como número plano sin color ni referencia.  
**Después**:

**En agent-card (lista izquierda)**:
- Dot semáforo de color junto al número:
  - 🟢 Verde: ≥ 80%
  - 🟡 Ámbar: ≥ 70% y < 80%  
  - 🔴 Rojo: < 70%
- Hint debajo: "vs 89% Dropi"
- `title` tooltip: "Tasa de éxito = acciones ejecutadas sin requerir intervención manual. Promedio Dropi: 89%."

**En agent-detail (panel derecho)**:
- Mismo dot semáforo + valor en color
- Barra de progreso horizontal con marcador vertical del benchmark (89% Dropi)
- Label "Prom. Dropi: 89%"

**Colores de agentes actuales**:
| Agente | Tasa | Semáforo |
|---|---|---|
| Roax | 94% | 🟢 Verde |
| Vigilante | 91% | 🟢 Verde |
| Chatea Pro | 88% | 🟢 Verde |
| ADA Spy | 79% | 🟡 Ámbar |
| Kronos | 96% | 🟢 Verde |

---

### 3. Panel "Umbrales de agencia" — accesible desde Hub Y Agentes

**Nuevo componente**: `GaliAgencyThresholdsPanelComponent`  
**Ruta**: `components/gali-agency-thresholds-panel/`  
**Activación**:
- Hub → botón "⚙ Umbrales" en el header de "Decisiones de hoy"
- Agentes page → botón "⚙ Umbrales de agencia" en el header de la página

**Estructura del panel**:
- Panel lateral derecho (width: 420px, height: 100vh, slide-in animation)
- Leyenda de niveles con badges de color:
  - 🟢 **Autónomo total** — actúa dentro de límites sin pedir permiso
  - 🟡 **Sugiere, yo apruebo** — propone y espera tu OK
  - 🔵 **Solo alertas** — solo notifica, nunca ejecuta

**5 agentes** con controles individuales:

| Agente | Color | Default | Permite cambio |
|---|---|---|---|
| Roax | #f97316 | Autónomo total | ✓ |
| Vigilante | #fbbf24 | Autónomo total | ✓ |
| Chatea Pro | #34d399 | Autónomo total | ✓ |
| ADA Spy | #818cf8 | Sugiere, yo apruebo | ✓ |
| Kronos | #60a5fa | 🔒 Bloqueado por diseño | ✗ |

Kronos tiene un estado especial "bloqueado" con mensaje: *"Por diseño, Kronos nunca ejecuta movimientos financieros sin tu clic de aprobación. Este límite no se puede modificar."* (GaliAjustesJun3 Bloque 8).

Cada agente muestra nota contextual según el nivel activo:
- Total: "✓ Actúa sin pedirte permiso dentro de tus límites en Reglas"
- Suggest: "⟳ Verás sus propuestas en 'Necesitas decidir' del Hub"
- Alerts: "🔔 Solo recibirás alertas — ninguna acción automática"

---

### 4. Hub — Decisiones separadas en dos bloques

**Antes**: una lista mezcla de signals `completed` y `pending_decision`.  
**Después**: dos bloques visuales diferenciados dentro de la sección:

**Bloque 1: "Gali ya lo hizo por ti"** (verde)
- Muestra signals con `estado === 'completed'`
- Opacidad reducida (0.75) → énfasis visual menor — solo informativo
- Dot verde 🟢

**Bloque 2: "Necesitas decidir"** (rojo)
- Muestra signals con `estado === 'pending_decision'`
- Badge rojo con conteo de decisiones urgentes
- Dot rojo 🔴
- Full opacity — requiere atención

**Acceso a Umbrales de agencia**: botón "⚙ Umbrales" en el header de la sección, abre el panel lateral sin navegar.

> **Integración Spec 3**: al reorganizar Hub en 3 zonas, mover estos bloques dentro de Zona 1 sin reescribir lógica.

---

### 5. "← Modo básico" — renombrado con tooltip

**Antes**: botón `← Vista simple` sin descripción.  
**Después**: botón `← Modo básico` con `title="Oculta los agentes y muestra solo métricas principales"`.

> **Spec 5** implementará el segmented control Vista simple/Experto en Zona 1. Este botón footer permanece como atajo secundario.

---

### Pendiente — verificación / backlog ⬜

| # | Tarea | Prioridad | Notas |
|---|---|---|---|
| V1 | Smoke test: click score 78 → panel lateral completo | Alta | Ver §Sprint de cierre — V1 |
| V2 | Smoke test: umbrales persisten en localStorage (si aplica) | Media | Ver §Sprint de cierre — V2 |
| V3 | Post-Spec 3: bloques CA-5 en Zona 1 superior | Alta | Ejecutar tras Spec 3 — ver §Sprint de cierre — V3 |
| — | Matriz **8 procesos × agencia** (AlcancesIADropi) | **Backlog** | Fuera alcance v1 — panel actual = 5 agentes |

---

## Sprint de cierre

### V1 — Smoke test: health panel completo

**Cuándo**: ejecutar con el prototipo activo en `ng serve`, antes de cerrar el gate de Spec 7.

**Pasos**:
1. Abrir Hub (`/gali-v5`) como dropshipper
2. Localizar el badge de score "78" en el header (`dropi-header-ia2`)
3. Hacer click en el score
4. Verificar que `health-panel` aparece como slide-in desde la derecha (sin salto ni parpadeo)
5. Confirmar cada elemento:
   - [ ] SVG ring visible con arco proporcional a 78/100 (≈ 78% del círculo)
   - [ ] Texto "78/100" prominente + label "Negocio saludable" en verde
   - [ ] Benchmark "82/100 Dropi" visible con delta "Estás 4 pts por debajo"
   - [ ] 4 filas de componentes con barra horizontal y marcador vertical del 82%
   - [ ] 2 recomendaciones con CTAs `[Ver smart routing →]` y `[Ver análisis Roax →]`
6. Click fuera del panel (backdrop) → panel se cierra sin error en consola
7. Reabrir el panel → click en CTA "Ver smart routing →" → panel se cierra y navega

**Resultado esperado**: panel abre/cierra limpiamente; ningún error en consola; `ringDash` calculado correctamente.

**Fallo típico y solución**:
- `ringDash` es `NaN` → verificar que `healthScore` está inicializado antes de `ngOnInit` y que la fórmula `163.4 * (score / 100)` se evalúa con número, no string.
- Panel no cierra con backdrop → verificar que `(click)` en `.health-panel-backdrop` llama `closeHealthPanel()` y no tiene `stopPropagation` incorrecto.

---

### V2 — Smoke test: umbrales en localStorage (opcional en prototipo)

**Cuándo**: ejecutar solo si el panel de umbrales guarda estado — si no hay persistencia implementada, documentar como deuda y marcar V2 aceptable.

**Pasos**:
1. Abrir panel "⚙ Umbrales" desde Hub (botón en header de "Decisiones de hoy")
2. Cambiar el nivel de **ADA Spy** de "Sugiere, yo apruebo" → "Solo alertas"
3. Cerrar el panel (backdrop click o botón ×)
4. Abrir DevTools → Application → Local Storage → `localhost:4200`
5. Buscar clave `gali-agency-thresholds`
   - [ ] Si existe: verificar que el valor de ADA Spy es `alerts`
   - [ ] Si no existe: el estado es in-memory — aceptable, documentar
6. Recargar la página (F5)
7. Reabrir el panel "⚙ Umbrales"
8. Verificar:
   - Con localStorage: ADA Spy sigue en "Solo alertas" ✅
   - Sin localStorage: ADA Spy vuelve a "Sugiere, yo apruebo" — aceptable para prototipo, registrar en backlog

**Resultado esperado**: cualquiera de los dos resultados es aceptable para el gate — lo que no es aceptable es que el panel no abra o que Kronos aparezca como editable.

---

### V3 — Post-Spec 3: bloques CA-5 en Zona 1 superior

> **Este smoke test se ejecuta DESPUÉS de implementar Spec 3.** No bloquea el gate de Spec 7, pero sí es requisito para el gate de Spec 3 (CA-9 de `3.HubNegocio.md`).

**Contexto**: Spec 3 reorganiza `home.component.html` en 3 zonas. Los bloques implementados por Spec 7 (`mission__signals-block--done` / `--pending`) deben quedar dentro de Zona 1, **antes** del insight banner "Gali esta semana".

**Pasos**:
1. Tras implementar Spec 3, abrir el Hub
2. Verificar visualmente que la parte superior de la pantalla (Zona 1) muestra ambos bloques
3. Verificar en DevTools → Elements el orden DOM dentro de Zona 1:

```
.zone-1  (o clase equivalente asignada en Spec 3)
  ├── .zone-1__header  (título + badge de conteo + botón ⚙ Umbrales)
  ├── .mission__signals-block--pending  (Necesitas decidir) ← full opacity
  ├── .mission__signals-block--done    (Gali ya lo hizo)   ← opacity 0.75
  └── .insight-banner                  (banner colapsable)
```

4. Con datos de mock activos:
   - [ ] `.mission__signals-block--pending` muestra señales con `estado === 'pending_decision'` con dot 🔴
   - [ ] `.mission__signals-block--done` muestra señales con `estado === 'completed'` con dot 🟢 y opacidad reducida
   - [ ] Badge del header refleja el conteo de `pendingApprovalSignals.length`
5. Confirmar que el botón "⚙ Umbrales" en el header de Zona 1 sigue abriendo `GaliAgencyThresholdsPanelComponent` (no se rompió al mover el HTML)
6. Confirmar que el botón "⚙ Umbrales" en `/agentes` sigue funcionando independientemente

**Resultado esperado**: bloques CA-5 visibles en la parte superior de Zona 1, funcionando sin regresión. El insight banner está debajo de los bloques, colapsado si hay pendientes.

---

### Contrato de integración Spec 7 → Spec 3 (DOM)

Cuando Spec 3 restructure `home.component.html`, los elementos de Spec 7 se reutilizan — **nunca se reimplementan**:

| Elemento Spec 7 | Selector CSS actual | Acción en Spec 3 |
|---|---|---|
| Bloque "Necesitas decidir" | `.mission__signals-block--pending` | Mover dentro de `.zone-1-body`, primer bloque |
| Cards `pendingApprovalSignals` | Hijos del bloque `--pending` | Sin cambios — permanecen como hijos |
| Bloque "Gali ya lo hizo" | `.mission__signals-block--done` | Mover dentro de `.zone-1-body`, segundo bloque |
| Cards `completedSignals` | Hijos del bloque `--done` | Sin cambios — permanecen como hijos |
| Botón "⚙ Umbrales" | `.mission__agency-btn` | Mover al header de Zona 1 junto al badge de conteo |
| `GaliAgencyThresholdsPanelComponent` | Instancia en `home.component.html` | Permanece fuera de zonas — es un overlay `position: fixed` |
| Variables `showAgencyPanel`, `completedSignals`, `pendingApprovalSignals` | `home.component.ts` | Sin cambios — Spec 3 agrega handlers pero no elimina estos |

**Regla de oro**: el selector `.mission__signals-block--pending` debe aparecer **antes** de `.mission__signals-block--done` en el DOM de Zona 1. El bloque de acción urgente (rojo) siempre precede al informativo (verde).

---

## Brechas adicionales — Gaps de CorrecionesGali5.md

### Brecha A — Página Agentes: explainer de jerarquía Gali → Agentes

**Problema (Bloque 1 #7)**: Los agentes Roax, Vigilante, ADA Spy, Chatea Pro y Kronos aparecen en la página de Agentes como entidades independientes con nombres propios. En el Hub y en Marketing aparecen mezclados con "Gali". El usuario no entiende si Gali incluye a todos estos agentes, si son herramientas separadas, o si son personas del equipo Dropi.

**Cambio en `agentes-page.component.html`**: Agregar en el header de la página un bloque explicativo "¿Cómo funciona esto?" accesible como tooltip, modal o sección expandible:

```html
<!-- Header de la página de Agentes -->
<div class="agentes-header">
  <h1>Agentes de Gali</h1>
  <button class="how-it-works-btn" (click)="showHowItWorks.set(true)">
    <i class="pi pi-question-circle"></i>
    ¿Cómo funciona esto?
  </button>
</div>

<!-- Explicación emergente -->
<div class="how-it-works-panel" *ngIf="showHowItWorks()">
  <button class="how-it-works-panel__close" (click)="showHowItWorks.set(false)">×</button>
  <h3>Gali y sus agentes</h3>
  <p>
    <strong>Gali</strong> es el orquestador central — tu Director de Operaciones IA que coordina todo el sistema.
    Roax, Vigilante, ADA Spy, Chatea Pro y Kronos son sus módulos especializados, cada uno con un área de expertise.
  </p>
  <p>
    Tú configuras cuánta autonomía tiene cada uno desde <strong>Umbrales de agencia</strong>.
    Gali decide cuándo activar cada módulo según el contexto de tu negocio.
  </p>
</div>
```

Los estilos siguen DS tokens (Inter, colores grises, borde `var(--surface-200)`). El panel puede ser un tooltip expandido o un drawer ligero.

**CA afectada**: CA-9 (nueva)

---

### Brecha B — Copy "Director de E-commerce" en el Hub header

**Problema (Bloque 2 #1 + Bloque 3)**: En ninguna pantalla hay un mensaje que diferencie a Gali de un dashboard tradicional. El texto "orquestando tu negocio" es genérico — no comunica que Gali actúa como un Director de E-commerce 24/7.

**Cambio en `dropi-header-ia2.component.ts/html`**: El subtítulo del estado del agente activo (actualmente "Gali · orquestando tu negocio") debe cambiar a frases que comuniquen rol estratégico:

| Contexto | Texto actual | Texto propuesto |
|---|---|---|
| Estado default | "Gali · orquestando tu negocio" | "Gali · tu Director de E-commerce activo" |
| Con decisiones pendientes | "Gali · orquestando tu negocio" | "Gali · hay 2 decisiones que necesitan tu aprobación" |
| Sin alertas activas | "Gali · orquestando tu negocio" | "Gali · negocio en orden · 0 alertas activas" |

Esta es una mejora de copy — no requiere lógica nueva, solo actualizar los strings en el servicio o en la interpolación del header.

Buscar en `dropi-header-ia2.component.ts` o `gali-state.service.ts` los strings de estado del header y aplicar los reemplazos.

**CA afectada**: CA-10 (nueva)

---

## Criterios de aceptación

- [ ] **CA-1**: Click en score header abre `health-panel` con ring, 4 componentes ponderados y recomendaciones.
- [ ] **CA-2**: Benchmark Dropi 82/100 visible con delta vs score usuario.
- [ ] **CA-3**: Agentes — semáforo + tooltip definición tasa éxito + "vs 89% Dropi".
- [ ] **CA-4**: Panel umbrales accesible desde Hub y `/agentes`; Kronos bloqueado.
- [ ] **CA-5**: Hub — bloques "Gali ya lo hizo" / "Necesitas decidir" separados visualmente con dot de color correcto (🔴 / 🟢) y opacidad diferenciada.
- [ ] **CA-6**: 0 instancias de "Vista simple" en UI visible (solo "Modo básico" en footer).
- [ ] **CA-7**: Tras Spec 3, `.mission__signals-block--pending` y `.mission__signals-block--done` están dentro de Zona 1, en ese orden DOM, **antes** del insight banner; botón "⚙ Umbrales" visible en el header de Zona 1. (V3 smoke test).
- [ ] **CA-8**: `ng build` sin errores.
- [ ] **CA-9**: Página Agentes muestra sección/botón "¿Cómo funciona esto?" que abre panel con explicación de jerarquía Gali → Agentes en 3 líneas (orquestador, módulos especializados, umbrales de autonomía).
- [ ] **CA-10**: Header de Hub reemplaza "orquestando tu negocio" por copy estratégico dinámico según estado del sistema (con decisiones pendientes / sin alertas / default "tu Director de E-commerce activo").

**Go Agent mode**: ✅ Verificación V1–V2. No bloquea Spec 2. V3 se ejecuta al cierre de Spec 3.

---

## Archivos modificados / creados

| Archivo | Tipo |
|---|---|
| `components/dropi-header-ia2.component.ts` | Reescrito — panel lateral health score completo, `healthComponents`, `healthRecs`, `ringDash` |
| `components/dropi-header-ia2.component.scss` | Añadido — `.health-panel-backdrop`, `.health-panel`, `.health-ring`, `.health-comp`, `.health-rec` |
| `components/gali-agency-thresholds-panel/` (3 archivos) | Creado — componente nuevo standalone |
| `pages/agentes/agentes-page.component.ts` | Añadido — `successRateColor()`, `successRateLabel()`, `successRateTooltip`, `DROPI_BENCHMARK`, `showAgencyPanel`, import del panel |
| `pages/agentes/agentes-page.component.html` | Añadido — `GaliAgencyThresholdsPanelComponent` + botón Umbrales + semáforo en agent-card y agent-detail |
| `pages/agentes/agentes-page.component.scss` | Añadido — `.success-rate-semaphore`, `.agent-card__benchmark-hint`, `.agent-stat__benchmark-bar`, `.agentes-page__btn-agency` |
| `home/home.component.ts` | Añadido — `showAgencyPanel`, `completedSignals`, `pendingApprovalSignals`, import del panel |
| `home/home.component.html` | Modificado — bloque doble decisiones + botón Umbrales + "Modo básico" + panel |
| `home/home.component.scss` | Añadido — `.mission__signals-block`, `.mission__signals-block-header`, `.mission__agency-btn`, etc. |

---

## Decisiones de diseño

**Por qué el health panel es lateral y no un modal central**: La heurística 3 (Control del usuario) requiere que el usuario pueda cerrar el panel sin perder el contexto del hub. Un panel lateral deja visible el dashboard detrás — el usuario puede tomar una decisión y volver inmediatamente al contexto donde estaba.

**Por qué Kronos está bloqueado en el panel de umbrales**: GaliAjustesJun3 Bloque 8 establece que Gali nunca mueve dinero sin aprobación explícita. Mostrar el control bloqueado (no ocultarlo) cumple la heurística 7 (Flexibilidad) y la heurística 3 (Control): el usuario ve que existe la opción pero entiende que este límite es una garantía de confianza, no una restricción arbitraria.

**Por qué el bloque "Gali ya lo hizo" tiene opacidad reducida**: La heurística de diseño estético establece que la información completada no debe competir visualmente con la información accionable. La opacidad 0.75 marca el bloque como "informativo" sin ocultarlo — el usuario puede revisarlo si quiere, pero su mirada va naturalmente al bloque rojo.

**Por qué el semáforo usa dots y no solo color en el número**: Los dots son redundantes (color + forma) — mejoran la accesibilidad para usuarios con daltonismo y crean un patrón de escaneo más rápido en la lista de agentes.
